# Directory Structure Generator Ver.2.9.0

## 主な変更

- プリセット管理画面でクイック表示の上限を1〜99件に設定できます（初期値5件）。上限を増やすと、表示順に未選択のプリセットも自動で追加されます。個別チェックで表示内容を調整でき、ボタンは横スクロールできます。
- 整理コピー後の保存先と、成功・スキップ・失敗・未実行件数をGUIへ表示します。
- 結果CSV、保存先フォルダー、保存後ファイルを整理コピー画面から開けます。
- コピー結果の記録形式をCSVまたはExcelブック（.xlsx）から選べます。初期設定はCSVです。
- Excelブックでは保存先・保存後ファイルへのリンクを表示します。
- 記録方法は「蓄積」または「初期化」を選べます。初期化では既存の同形式ログを日付付きファイルへ退避してから今回分を記録します。
- 結果履歴の保存先をコピー先とは別に指定でき、次回起動時も設定を保持します。
- CSV／Excelと蓄積／初期化の選択を、選択状態が分かりやすいトグル風ボタンに変更しました。
- EXEビルド用BATが起動元の作業フォルダーに依存しないよう修正しました。Windowsテストで保存先ショートカット（.lnk）を正しく確認します。
- `_DirectoryStructureGenerator_copy_log.csv` をExcel対応の18列形式へ拡張しました。
- CSVはUTF-8 BOM、CRLF、追記方式で、見出しを重複させません。
- Excel用リンクとパーセントエンコード済みfile URIを併記します。
- Windowsでは日本語フォルダー名を保てる `保存先を開く.lnk` をコピー先に生成します。
- 整理コピーの設定欄と計画表の境界をドラッグして、一覧の高さを変更できます。
- 整理コピー画面を最大化・最小化し、ウィンドウ端からサイズ変更できます。
- 成功、失敗、スキップ、中止と、代表的なコピーエラー分類を記録します。
- ユーザー由来セルに対するCSV数式インジェクション対策を追加しました。
- 選定済みアイコンをWindows EXEと操作マニュアル表紙へ適用しました。
- 整理コピー画面で画面幅に合わせて計画表の列幅を調整します。

## 互換性と安全性

- フォルダー構造生成、プレビュー、連番、検査、設定の動作は変更していません。
- `execute_copy_plans()` の呼び出し方法と戻り値は維持しています。
- 旧5列CSVがコピー先にある場合は削除せず、日時付きの `_legacy_` CSVへ退避してから新形式を開始します。
- 元ファイル削除は従来どおり、明示選択と最終確認後、コピー成功・保存先存在・サイズ一致を満たす場合だけ実行します。

## 実機確認について

Windows Explorer、UNC共有、Microsoft Excelでのリンク動作と表示は、Windows実機での最終確認が必要です。
