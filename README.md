# Directory Structure Generator Ver.2.9.1

<img src="docs/DirectoryStructureGenerator_AppIcon_512.png" width="128" alt="Directory Structure Generator icon">

フォルダー構成を走査し、必要な項目だけを **TXT / HTML / CSV / JSON** に出力するWindows向けGUIアプリです。

- Version: 2.9.1
- Updated: 2026-09-25
- Author: hiro1960
- UI: PySide6
- 対応OS: Windows 10 / 11（64ビット推奨）
- Python: 3.10以上（Python 3.13対応）

## 今回の主な改善

- 整理コピー後に、保存先、成功・スキップ・失敗・未実行件数を画面で確認可能
- 「結果CSVを開く」「保存先を開く」「保存後ファイルを表示」を整理コピー画面へ追加
- コピー結果CSVをExcel向けのUTF-8 BOM・CRLF・18列形式へ拡張
- コピー結果CSVにExcelリンク、パーセントエンコード済みURI、エラー分類を記録
- コピー先に `保存先を開く.url` を生成
- 既存の旧形式CSVは、初回のみ日時付きの `_legacy_` ファイルへ退避
- ファイル名・パス・エラー文字列に対するCSV数式インジェクション対策を追加
- 選定済みアプリアイコンをWindows EXEと操作マニュアルへ適用
- 整理コピー画面で画面幅に合わせて列幅を調整

- 整理コピーのファイル名と拡張子を分離し、拡張子を候補選択または手入力可能に改善
- メイン画面のフィルター領域を広げ、小さい画面でもスクロールして全項目を確認可能に改善

- 起動時に使う「基本プリセット」を1件指定
- 「よく使う設定」の表示上限を1〜99件で設定でき、表示順も指定可能（初期値5件）
- メイン画面からユーザープリセットを確認付きで更新
- 設定変更後に「変更あり（未保存）」を表示
- CSVで基本・クイック表示・表示順を管理（Ver.2.7 CSVも読み込み可能）
- 除外フォルダー、拡張子、パターンの重複を保存時に自動整理

- 用途別の名前付き設定プリセットを追加
- 開発、写真、動画・音声、文書、全ファイル、高速走査の標準プリセットを収録
- 常用プリセットを画面上部へ表示（初期上限5件、管理画面で1〜99件に変更可能）
- プリセットごとに用途・対象・注意点のメモを保存
- 現在の設定から新規作成、複製、名前・メモ編集、削除に対応
- プリセットをUTF-8 BOM付きCSVへ書き出し・読み込み
- CSVの同名プリセットは確認後に更新し、不正な行はエラーとして停止
- 安全のため、元ファイル削除モードはプリセットやCSVに保存しない

- 一覧の列見出しクリックで昇順・降順を切り替え
- 対象列を選んで文字列フィルターを適用
- 列見出しの右クリックでソート・フィルター・列幅調整
- 一覧の右クリックでファイルを開く、保存場所を開く、パスコピー、整理コピー対象への追加、ファイル情報表示
- 右クリックメニューから元ファイルを直接削除・リネームしない安全設計

- 「ファイル詳細」「整理コピー」の両タブで同じプレビューを常時表示
- 境界をドラッグしてプレビュー幅を変更可能
- 整理コピー側の行選択から画像・動画・音声プレビューへ連動
- ダークテーマ時のプレビュー背景・文字・枠線のコントラストを改善
- 既定は従来どおり元ファイルを保持し、コピー側だけを整理
- 任意で「コピー成功後に元ファイルを削除」を選択可能
- 元ファイル削除前の最終確認と、コピー前後のサイズ一致確認を追加
- 拡張子だけが変更される場合、内容形式は変換されないことを警告
- 画像・動画・音声・文書・その他を複数同時に選べるチップ型フィルター
- 検索結果に連動する件数バッジと、条件クリア／適用ボタン
- ヘッダーの三状態チェック、選択件数／表示件数の常時表示
- Shiftクリック範囲選択、Ctrl+A、フィルター変更時の安全な選択解除
- MP3 / WAV / FLAC / AAC / M4A / OGG等の音声解析・再生
- ICO画像の解析・プレビューと、見やすくした動画・音声シークバー
- 文書・コードは描画せず、メタ情報だけを表示する安定重視の設計
- 大量ファイルを整理コピーする前の件数・コピー先確認
- ファイル名と保存場所を分離した、列幅変更可能な一覧表示
- チェック式の複数選択と、表示中の全選択・解除・反転
- 画像プレビューの安定化と、動画の再生・シーク・詳細情報表示
- コピー側の新しい名前を元ファイル名と比較しながら1件ずつ編集
- 初心者向けの選択式命名ルールと、整理コピーの既定設定画面
- 新しい保存フォルダーの作成と、コピー先のドラッグ＆ドロップ
- 「ファイル詳細・整理コピー」を独立したポップアップ画面として追加
- 一般ファイルのサイズ、更新日時、作成日時、行数、アクセス権、読取専用属性を表示
- PNG / JPEG / GIF / BMP / WebP / TIFF / SVG / ICOの解像度、縦横比、カラーモードを表示
- Exif、カメラ機種、撮影日時を任意取得。GPSは個人情報保護のため別指定・初期オフ
- サムネイル、ファイル検索、一般／画像フィルター、元ファイル・保存場所を開く機能
- 任意のSHA256取得と、詳細CSV / JSONレポート出力
- 選択ファイルを事前指定先へ安全に整理コピー
- 整理コピー先は、入力欄または整理コピー画面へのフォルダーのドラッグ＆ドロップで指定可能
- コピー時の新ファイル名・命名ルール、連番、日付、元のフォルダー構成維持に対応
- 同名ファイルは自動連番またはスキップ。既存ファイルを上書きしない設計
- 元データ側へのコピーを禁止し、コピー結果をCSVログへ記録
- Windows 11になじむダーク／ライト対応UI
- フォルダー入力欄と画面全体へのドラッグ＆ドロップ
- フィルター部分だけをスクロールし、出力形式と実行ボタンを常時表示
- 大きなフォルダーでも画面を固めにくいバックグラウンド走査
- 大量データ時のプレビュー表示を15,000行に制限（出力ファイルは全件）
- Windowsドライブで異なるフォルダーを同一と誤判定する問題を修正
- 走査の中止
- ツリーの事前プレビューと検索・コピー
- フォルダー数、ファイル数、合計容量、除外数、エラー数の表示
- 出力先が対象フォルダー内にある場合、その出力先を自動除外
- Excelで開きやすいUTF-8 BOM付きCSV
- CSV数式インジェクション対策
- 設定と直近10件のフォルダー履歴をユーザー領域へ保存

## 設定プリセット

画面上部の「設定プリセット」から用途別の設定を選択し、「適用」を押します。「プリセット管理」を開き、上部の「クイック表示の上限」で1〜99件を設定できます（初期値5件）。上限を増やすと、表示順に未選択のプリセットも自動で追加されます。各プリセットの「クイック」チェックで個別に変更できます。ボタンが横幅を超える場合は横スクロールできます。プリセットを適用しただけでは走査やコピーは開始されません。

「管理…」では、現在の対象・出力先、フィルター、出力形式、整理コピー設定を名前とメモ付きで保存できます。標準プリセットは削除できませんが、複製して自由に編集できます。

CSVへ書き出すと別PCへの移行やバックアップに利用できます。CSVの`favorite`列を`true`または`false`に変更すると常用指定も変更できます。読み込み時は内容と件数を確認し、同名のプリセットだけを更新します。

> 安全のため「コピー成功後に元ファイルを削除」はプリセットへ保存されません。プリセット適用後も必要な場合だけ整理コピー画面で選択し、最終確認します。

## フィルター機能

初期設定の「開発用おすすめ」では、次のような巨大・一時フォルダーを除外します。

```text
.git                 Git内部データ
node_modules         JavaScript依存パッケージ
.venv / venv / env   Python仮想環境
__pycache__          Pythonキャッシュ
.vs / bin / obj      Visual Studio・.NET生成物
dist / build         ビルド出力
.pytest_cache        テストキャッシュ
.idea                IDE設定
target               Rust・Javaなどの生成物
```

次の条件も組み合わせられます。

- 除外フォルダー：1行に1つ指定
- 除外拡張子：`.log, .tmp, .pyc` のように指定
- 対象拡張子：`.py, .md` のように指定。空欄ならすべて
- 除外パターン：`*_backup, *.egg-info, temp/*` のようなワイルドカード
- 隠しファイルを含める／含めない
- 空フォルダーを含める／含めない
- 最大階層
- シンボリックリンクをたどる／たどらない

> シンボリックリンクをたどる設定は、リンクが循環しているフォルダーでは使用しないでください。

## まず試す

1. ZIPを右クリックし、**「すべて展開」**します。
2. 展開したフォルダーの `START_APP.bat` をダブルクリックします。
3. 初回は専用環境とPySide6を自動準備するため、数分かかる場合があります。
4. 「対象フォルダー」と「出力先」を選びます。
5. まず「プレビュー走査」を押します。
6. 内容を確認して「構成ファイルを生成」を押します。

## ファイル詳細・整理コピー

1. 対象フォルダーを選びます。
2. 左側の「ファイル詳細・整理コピー…」を押します。
3. 「1. ファイル詳細」で必要な取得項目を選び、「詳細を解析」を押します。
4. 一覧から整理したいファイルを選択します。
5. 「2. 整理コピー」でコピー先と新しい名前・命名ルールを指定します。コピー先は「参照…」のほか、エクスプローラーから入力欄または画面へフォルダーをドラッグ＆ドロップして指定できます。
6. 「元ファイルの扱い」を選びます。通常は「元ファイルを保持する（推奨）」のまま使用してください。
7. 「コピー結果ファイル」でCSVまたはExcelブック（.xlsx）を選び、「記録方法」で「蓄積」または「初期化」を選びます。「結果履歴の保存先」で記録用フォルダーを指定できます。空欄ならコピー先へ保存します。初期化では指定先の既存ログを日付付きファイルへ退避し、今回分だけの新しいログを作ります。
8. 「コピー内容を事前確認」で、コピー先を確認します。
9. 「確認した内容で整理コピー」を押します。
10. 完了後は「前回の整理結果」で件数と保存先を確認し、結果ファイル・保存先・保存後ファイルを開けます。

結果記録は既定で蓄積されます。CSVを選ぶと `_DirectoryStructureGenerator_copy_log.csv`、Excelブックを選ぶと `_DirectoryStructureGenerator_copy_log.xlsx` に記録します。保存先はコピー先とは別のフォルダーに指定でき、設定は次回も保持されます。空欄の場合のみコピー先に記録します。初期化を選ぶと、指定した履歴フォルダーの既存ログを日付付きの履歴ファイルへ退避してから今回の結果だけを記録します。Windowsではコピー先に日本語パス対応の `保存先を開く.lnk` も作成します。

一覧の列見出しをクリックすると、その列で昇順・降順を切り替えられます。検索欄の左で対象列を選ぶと、指定列だけを部分一致で絞り込めます。列見出しを右クリックすると、並べ替え・絞り込み・列幅調整を選べます。一覧のファイルを右クリックすると、ファイルを開く、保存場所を開く、パスのコピー、整理コピー対象への追加、ファイル情報表示を利用できます。

「GPSを含める」は個人情報保護のため初期状態ではオフです。整理操作は既定でコピー専用です。「コピー成功後に元ファイルを削除する」を明示的に選んだ場合だけ、コピー成功・サイズ一致・最終確認の後に元ファイルを削除します。コピー先には対象フォルダーの外側だけを指定できます。

## 操作マニュアル（PDF）

- 現在の公開版：[Directory Structure Generator Ver.2.9.0 操作マニュアル](docs/DirectoryStructureGenerator_Ver2.9.0_操作マニュアル.pdf)
- 編集用：[Directory Structure Generator Ver.2.9.0 操作マニュアル（PowerPoint）](docs/DirectoryStructureGenerator_Ver2.9.0_操作マニュアル.pptx)
- 画面画像付きで、初回起動、列のソート・フィルター、右クリック操作、メディア確認、整理コピー、安全設定を説明しています。
- Ver.2.9.0で追加した整理コピー結果の確認方法と、Excel用コピー結果CSVの見方も掲載しています。
- [Ver.2.9.0 受け入れテスト結果（CSV）](docs/Ver2.9.0_ACCEPTANCE_TEST_RESULTS.csv)

`START_APP.bat`はCMD画面を勝手に閉じません。起動に失敗した場合は、同じフォルダーの`startup_error.log`に原因を保存して画面にも表示します。

> ZIPを開いた画面からBATを直接実行しないでください。必ず「すべて展開」してから使用します。

## EXEを作る

1. `build_exe.bat` をダブルクリックします。
2. `.venv`や必要なパッケージがなければ、自動的に準備されます。
3. 自動テスト後、`dist\DirectoryStructureGeneratorGUI` にEXE一式ができます。
4. `release`に配布用ZIPとZIPのSHA256ができます。

EXEはPyInstallerの`onedir`形式です。`DirectoryStructureGeneratorGUI.exe`だけを取り出さず、フォルダー一式で配布してください。EXEのハッシュは`SHA256_EXE.txt`へ、配布ZIPのハッシュは`.sha256.txt`へ自動出力されます。

動作確認用にEXEだけを素早く作る場合は、`BUILD_FAST.bat`を使用します。配布ZIPは作成しません。完全に作り直す必要がある場合だけ、コマンドプロンプトから`build_exe.bat clean`を実行します。

## ショートカット

| キー | 動作 |
|---|---|
| `Ctrl+P` | プレビュー走査 |
| `Ctrl+G` | 構成ファイルを生成 |
| `Ctrl+F` | プレビュー検索欄へ移動 |

## 出力ファイル

対象フォルダー名が`MyProject`の場合、次の名前で保存します。

```text
MyProject_tree.txt
MyProject_index.html
MyProject_file_list.csv
MyProject_tree_data.json
MyProject_file_details.csv
MyProject_file_details.json
```

HTMLには「すべて開く」「すべて閉じる」「名前を検索」があり、単体で閲覧できます。

## 設定の保存場所

設定は配布フォルダーを汚さず、次のユーザー領域に保存します。

```text
%APPDATA%\DirectoryStructureGenerator\config.json
```

設定を初期化したい場合は、アプリ終了後にこのファイルを削除してください。

## フォルダー構成

```text
app.py                 起動ファイル
dsg_app/               アプリ本体
dsg_app/file_inspector.py      一般ファイル・画像の詳細解析
dsg_app/organizer.py           安全な整理コピー／任意のコピー後削除
dsg_app/file_manager_dialog.py 詳細・整理コピー画面
tests/                 自動テスト
setup_dev.bat           初回セットアップ
START_APP.bat            推奨起動（自動準備・エラー記録）
run_app.bat              START_APP.batへの互換入口
run_tests.bat           自動テスト
build_exe.bat           EXE作成
requirements.txt        実行用ライブラリ
requirements-build.txt  ビルド・テスト用ライブラリ
```

## 改造・再配布・共同開発

本プロジェクトは **MIT License** で公開しています。LICENSEの条件に従い、個人・団体を問わず、利用、複製、改造、再配布、商用利用ができます。

- 独自の改造版を作る場合は、このリポジトリをForkして自由に開発できます。
- 改良内容を本リポジトリへ提案する場合は、IssueまたはPull Requestを利用してください。
- Pull Requestは内容と動作を確認したうえで、公式版へ取り込むか判断します。
- 著作権表示とMIT Licenseの表示は、コピーや再配布物にも残してください。
- 第三者が公開したFork版・改造版は、本プロジェクトの公式版ではありません。
- 第三者版の動作保証、問い合わせ対応、損害への責任を原作者は負いません。
- 公式配布物は、このリポジトリのGitHub Releasesで公開したものを基準とします。

## GitHub リポジトリと公式配布

- リポジトリ（ソースコード・更新履歴・Issue）：https://github.com/hiro1960486/DirectoryStructureGenerator
- Releases（利用者向けWindows配布ZIP）：https://github.com/hiro1960486/DirectoryStructureGenerator/releases
- 公式リリースから取得したZIPを使用してください。ソースから利用する場合は、READMEの開発環境手順を確認してください。
- 不具合や改善案はGitHubのIssues、コード変更の提案はPull Requestから受け付けます。

## 注意事項

- 本ツールの一覧・レポート機能はファイルを読み取り専用で調査します。
- 整理機能は既定でコピー専用です。元ファイルを保持する設定を推奨します。
- 「コピー成功後に元ファイルを削除する」を選んだ場合は実質的な移動になります。最終確認後の削除は元に戻せません。
- 拡張子を変えてもファイル内容の形式は変換されません。警告内容を確認してから実行してください。
- GPS情報を扱う場合は、レポートの共有範囲に注意してください。
- アクセス権がないフォルダーはエラー件数として記録し、可能な範囲で処理を続けます。
- 出力先は対象フォルダーと同じ場所には指定できません。
- 署名なしEXEでは、Windows SmartScreenの確認画面が表示される場合があります。

## 起動できないとき

1. CMD画面を閉じず、最後に表示されたエラーを確認します。
2. アプリと同じフォルダーの`startup_error.log`を開きます。
3. アプリ内部のエラーは`%LOCALAPPDATA%\DirectoryStructureGenerator\app_error.log`にも保存されます。
4. 相談するときは、このログを添付してください。
